# OpenSpec — My-first-Holy-Communion
# Auto-generated by sdd-init on 2026-06-19
# Mode: hybrid (files + Engram)

project: My-first-Holy-Communion
stack:
  - React (planned)
  - Vite (planned)
  - Tailwind CSS (planned)
  - TypeScript (planned)
  - Node.js (planned)
strict_tdd: false
test_runner: none
testing:
  strict_tdd: false
  runner:
    available: false
    reason: No test runner installed. Project is in pre-setup state.
  layers:
    unit: { available: false, tool: '—' }
    integration: { available: false, tool: '—' }
    e2e: { available: false, tool: '—' }
  coverage:
    available: false
    command: '—'
  quality:
    linter: { available: false, command: '—' }
    type_checker: { available: false, command: '—' }
    formatter: { available: false, command: '—' }
context: |
  Greenfield React + Vite + Tailwind project. No source code yet.
  Target: First Holy Communion management app (guests, sacraments, events).
  Stack to be bootstrapped from scratch.
phase_rules:
  propose:
    - Must specify delivery_strategy (single PR or chained) given >400-line guard.
  spec:
    - Include acceptance criteria for each user-facing behavior.
  design:
    - Prefer React functional components with hooks.
    - Use Tailwind for styling — no CSS modules or styled-components unless justified.
  tasks:
    - Keep each task testable independently.
  apply:
    - Keep tests with code in the same commit.
    - Follow work-unit-commits conventions.
  verify:
    - Verify acceptance criteria from spec.
  archive:
    - Sync delta specs back to openspec/specs/.
